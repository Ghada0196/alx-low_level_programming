#include "main.h"

/**
 * int _abs - check description
 * @r: an input integer
 * description:  a function that computes the absolute value of an integer
 * Return: return absolute value of an integer
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	return (r * -1);
}
